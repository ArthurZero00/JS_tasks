/*
Task 9: Split a String into an Array
Description:
Split the string message into an array of words (splitting by spaces). 
Then print the resulting array. 
*/

const message = "I promise to study well";
const msg_arr = message.split(" ");
console.log(msg_arr);